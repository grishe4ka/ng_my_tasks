import { TaskShortenerPipe } from './task-shortener-pipe';

describe('TaskShortenerPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskShortenerPipe();
    expect(pipe).toBeTruthy();
  });
});
